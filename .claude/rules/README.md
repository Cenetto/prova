# Rules

Cartella per le regole dettagliate di sviluppo del progetto (es. sicurezza, stile del codice, convenzioni architetturali).

Ogni file Markdown qui dentro descrive un insieme di regole che Claude deve seguire durante lo sviluppo.

Esempio di struttura:

```
rules/
  security.md
  code-style.md
```
